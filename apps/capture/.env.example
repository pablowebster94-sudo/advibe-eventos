# Backend al que la PWA sube las fotos.
# Para probar desde el celular esto NO puede ser localhost: usa la IP LAN
# del Mac (p. ej. http://192.168.1.7:3300) o la URL del túnel HTTPS.
NEXT_PUBLIC_BACKEND_URL="http://localhost:3300"
